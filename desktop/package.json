{
  "name": "ios-imap-notes-offline",
  "productName": "iOS IMAP Notes Offline",
  "version": "1.4.6",
  "description": "Offline desktop notes editor companion for iOS IMAP Notes",
  "author": "Christian Eichert",
  "license": "GPL-3.0-only",
  "main": "main.js",
  "scripts": {
    "check": "node --check apple-note.js && node --check imap-service.js && node --check note-parser-worker.js && node --check update-utils.js && node --check main.js && node --check preload.js && node --check search-utils.js && node --check paste-utils.js && node --check tab-utils.js && node --check renderer.js",
    "test": "node --test test/*.test.js",
    "dist:dir": "electron-builder --dir",
    "dist:win": "electron-builder --win nsis --publish never",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "44.0.0",
    "electron-builder": "26.15.3"
  },
  "build": {
    "appId": "net.siliconvenice.iosimapnotes.offline",
    "asar": true,
    "publish": [
      {
        "provider": "github",
        "owner": "ecxod",
        "repo": "iOS-IMAP-Notes"
      }
    ],
    "files": [
      "main.js",
      "apple-note.js",
      "imap-service.js",
      "note-parser-worker.js",
      "update-utils.js",
      "preload.js",
      "search-utils.js",
      "paste-utils.js",
      "tab-utils.js",
      "renderer.js",
      "index.html",
      "styles.css",
      "vendor/**/*"
    ],
    "artifactName": "ios-imap-notes-offline-${version}-${arch}.${ext}",
    "directories": {
      "output": "dist"
    },
    "nsis": {
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "oneClick": false,
      "perMachine": false
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    }
  },
  "dependencies": {
    "electron-updater": "6.8.9",
    "imapflow": "1.7.6",
    "mailparser": "3.9.16",
    "nodemailer": "9.0.5"
  }
}
